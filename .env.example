# Firebase service account JSON (inline or file path)
FIREBASE_SERVICE_ACCOUNT_JSON=
# Optional: rate limit per device per day
RATE_LIMIT_PER_DEVICE_PER_DAY=500
# Server host/port
HOST=0.0.0.0
PORT=8000
